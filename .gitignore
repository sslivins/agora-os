# Build outputs
dist/
out/
work/
*.img
*.img.zst
*.img.xz
*.sig

# Pi-gen working tree (cloned/pinned in CI)
pi-gen/

# Local secrets — never commit
*.pem
*.key
!image-build/*-pubkey.pem.example
# Baked update-verification pubkeys are public by design (D54).
# Private *.key files remain ignored.
!image-build/update-pubkey.pem
!image-build/update-pubkey-recovery.pem

# Editor scratch
*.swp
*~
.DS_Store
